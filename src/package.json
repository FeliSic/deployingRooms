{
  "name": "src",
  "version": "1.0.0",
  "license": "MIT",
  "dependencies": {
    "@vaadin/router": "^2.0.0",
    "dotenv": "^17.2.2",
    "firebase": "^12.2.1",
    "parcel": "^2.15.4"
  },
  "scripts": {
    "dev": "parcel serve ./index.html",
    "build": "parcel build ./index.html",
    "ci": "yarn install && yarn build",
    "start": "serve dist"
  },
  "devDependencies": {
    "process": "^0.11.10",
    "serve": "^14.2.5"
  }
}
